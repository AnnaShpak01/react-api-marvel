import { observer } from 'mobx-react'
import ProducersListVM from './ProducersListVM'

function ProducersList({ vm }: { vm: ProducersListVM }) {
  return <div> ProducersList</div>
}

export default observer(ProducersList)
